import { MedicineDuePipe } from './medicine-due.pipe';

describe('MedicineDuePipe', () => {
  it('create an instance', () => {
    const pipe = new MedicineDuePipe();
    expect(pipe).toBeTruthy();
  });
});
